%table
  %tr
    %th Name
    %th Depends
    %th MD5Sum
    %th Maintainer
    %th Versions
  - @packages.each do |package|
    %tr
      %td= package.name
      %td= package.depends
      %td= package.md5_sum
      %td= package.maintainer
      %td= package.versions.map(&:number)
